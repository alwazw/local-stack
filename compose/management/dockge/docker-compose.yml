# version: "3.8"

services:
  dockge:
    image: louislam/dockge:latest
    container_name: dockge
    restart: unless-stopped
    profiles: [management]
    environment:
      DOCKGE_STACKS_DIR: /opt/stacks
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - dockge_data:/app/data
    ports:
      - "127.0.0.1:${PORT_DOCKGE:-5001}:5001"
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dockge.rule=Host(`dockge.${DOMAIN}`)"
      - "traefik.http.routers.dockge.entrypoints=websecure"
      - "traefik.http.routers.dockge-web.entrypoints=web"
      - "traefik.http.routers.dockge-web.rule=Host(`dockge.${DOMAIN}`)"
      - "traefik.http.routers.dockge.tls.certresolver=cloudflare"
      - "traefik.http.services.dockge.loadbalancer.server.port=5001"
      - "homepage.group=Management"
      - "homepage.name=Dockge"
      - "homepage.icon=dockge.png"
      - "homepage.href=https://dockge.${DOMAIN}"
      - "homepage.description=Docker Compose management UI"

networks:
  proxy:
    external: true

volumes:
  dockge_data:
    external: true
