# version: "3.8"

services:
  searxng:
    image: searxng/searxng:latest
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
        window: 120s
      resources:
        limits:
          cpus: "2.0"
          memory: 1G
    profiles: [ai]
    environment:
      SEARXNG_BASE_URL: "http://searxng:8080/"
      SEARXNG_SECRET_KEY_FILE: /run/secrets/webui_secret_key
    volumes:
      - ./settings.yml:/etc/searxng/settings.yml:ro
      - ./limiter.toml:/etc/searxng/limiter.toml:ro
    networks:
      - proxy
      - ai-ml
    secrets:
      - webui_secret_key
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.searxng.rule=Host(`search.${DOMAIN}`)"
      - "traefik.http.routers.searxng.entrypoints=websecure"
      - "traefik.http.routers.searxng-web.entrypoints=web"
      - "traefik.http.routers.searxng-web.rule=Host(`search.${DOMAIN}`)"
      - "traefik.http.routers.searxng.tls.certresolver=cloudflare"
      - "traefik.http.services.searxng.loadbalancer.server.port=8080"
      - "homepage.group=AI"
      - "homepage.name=SearXNG"
      - "homepage.icon=searxng.png"
      - "homepage.href=https://search.${DOMAIN}"
      - "homepage.description=Privacy-respecting metasearch"

